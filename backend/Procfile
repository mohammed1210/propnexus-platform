# Web API process (what Railway should run)
web: uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}

# Optional worker. Comment out or keep if you run a second worker service.
# worker: bash -lc './scripts/cron-ingest.sh'

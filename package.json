{
  "name": "propnexus-platform-root",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "frontend:ci": "npm --prefix frontend ci",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:lint": "npm --prefix frontend run lint",
    "backend:deps": "python -m pip install -r backend/requirements.txt",
    "precommit": "python -m pip install pre-commit && pre-commit run --all-files --show-diff-on-failure",
    "ci": "npm run frontend:ci && npm run frontend:build && npm run backend:deps && npm run precommit"
  }
}

const {
  id,
  title,
  price,
  location,
  bedrooms,
  bathrooms,
  description,
  imageurl,
  yield_percent,
  roi_percent,
  source,
  bmv,              // ✅ ADD this if using
  propertyType,
  investmentType,
  latitude,
  longitude,
} = property;

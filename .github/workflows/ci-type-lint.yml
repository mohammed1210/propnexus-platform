name: CI – Typecheck & Lint

on:
  workflow_dispatch:
    branches: ['sprint-*']

jobs:
  type_lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
      - run: npm ci || npm i
      - run: npx tsc --noEmit
      - run: npx next lint

name: Backend CI

on:
  push:
    branches: [ main, po2 ]
  pull_request:

jobs:
  backend:
    name: Backend CI
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        working-directory: backend
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then
            pip install -r requirements.txt
          fi
          pip install ruff black pytest

      - name: Lint (ruff + black --check)
        working-directory: backend
        run: |
          ruff check .
          black --check .

      - name: Run tests (if any)
        # Put repo root on PYTHONPATH so `from backend.main import app` works
        env:
          PYTHONPATH: ${{ github.workspace }}
        working-directory: backend
        run: |
          if ls -1 *_test.py tests 2>/dev/null | grep -q '.'; then
            echo "Running pytest..."
            pytest -q
          else
            echo "No tests found, skipping pytest"
          fi

      # Optional: quick HTTP smoke against the running dev server (uvicorn)
      # - name: Smoke Start (background)
      #   working-directory: backend
      #   env:
      #     PORT: 8000
      #   run: |
      #     python -m uvicorn main:app --host 127.0.0.1 --port $PORT &> uvicorn.log &
      #     for i in {1..30}; do
      #       if curl -fsS http://127.0.0.1:$PORT/health >/dev/null 2>&1; then
      #         echo "API is up"; break
      #       fi
      #       sleep 1
      #     done
      # - name: Smoke: GET /health
      #   run: curl -fsS http://127.0.0.1:8000/health